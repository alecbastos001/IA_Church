{
  "name": "ia_church",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "4.0.3"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  },
  "scripts": {
  "start": "react-scripts start",
  "build": "set \"NODE_OPTIONS=--openssl-legacy-provider\" && react-scripts build",
  "predeploy": "npm run build",
  "deploy": "gh-pages -d build"
},
  "homepage": "https://alecbastos001.github.io/IA_Church",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
